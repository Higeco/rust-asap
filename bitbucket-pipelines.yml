image: rust:latest

pipelines:
  default:
    - parallel:
        - step:
            name: Build and test
            script:
              - cargo build
              - cargo test
        - step:
            name: Run benchmarks
            script:
              - cargo bench
