image: rust:latest

pipelines:
  default:
    - step:
        script:
          - echo "Build project"; cargo build
          - echo "Running tests"; cargo test
          - echo "Running benchmarks"; cargo bench
